# Workshop n8n
## Prerequisites
* docker
* docker-compose

```sh
# mac
brew install colima docker docker-compose

# ubuntu 
curl -fsSL https://get.docker.com | sh
```

## Run the n8n docker
```sh
docker-compose up -d
```
